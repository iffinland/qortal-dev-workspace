# Implementation — <one main problem>

## Prerequisites

## Scope

## Out of scope

## Acceptance criteria

## Implementation constraints

## Required validation levels

List static, automated, build/runtime, real-host and live-resource checks that
apply. For any feature that renders untrusted or stored content (plain text is
still untrusted), include malicious HTML/URL fixtures: script tags,
event-handler attributes and unsafe `javascript:`/`data:` URLs, including
post-sanitization transformation cases.

## Owner/live validation requirement

## Completion and closure rule

Completion evidence MUST be reported. Issue closure and external mutations
require explicit owner authorization.
