# Project — <name>

## Project identity

## Repository and local path

## Product purpose

## Current state

Separate verified state, inference, unknowns and owner decisions. Date
operational snapshots and do not present an old Git/worktree state as current
after the repository advances.

## QDN identities and services

Record the publishing name, service (`APP`/`WEBSITE`), identifier, and the
identifier prefix used for app data.

## Entity and operation model

## Authority and trust boundaries

Record how owner detection is performed (never a hardcoded name/address) and
what moderation can and cannot do. Rendering safety is a separate gate from
schema validation and publisher authority; record the project's sanitizer/editor
choice and the malicious HTML/URL fixtures used to validate rich text.

## Qortal platform dependencies

Record the checked-out Core/Hub/framework revisions and re-verification
requirements.

## Key commands

## Validation

Record the node/endpoint and host context used, how to run local development
(Core dev proxy), and what must be verified in a real host. Do not encode one
node port as a timeless platform constant.

## Known limitations

## Current reference revisions

## Mandatory project rules
